import { describe, expect, it } from "vitest";
import { InMemoryEventSink, RedactingEventSink } from "../../packages/protocol/src/index.js";
describe("event redaction", () => it("removes secret-shaped fields before persisting", async () => { const memory = new InMemoryEventSink(); await new RedactingEventSink(memory).append({ id: "1", sessionId: "s", turn: 0, type: "agent.started", timestamp: "now", data: { apiKey: "sk-example-secret-value", authorization: "Basic anything", detail: "Bearer abcdefghijklmnopqrstuvwxyz AWS AKIAIOSFODNN7EXAMPLE GitHub ghp_abcdefghijklmnopqrstuvwxyz123456" } }); const events = JSON.stringify(memory.events); expect(events).not.toContain("secret-value"); expect(events).not.toContain("AKIAIOSFODNN7EXAMPLE"); expect(events).not.toContain("ghp_abcdefghijklmnopqrstuvwxyz123456"); expect(events).toContain("[REDACTED]"); }));
