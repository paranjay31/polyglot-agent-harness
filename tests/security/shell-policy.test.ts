import { describe, expect, it } from "vitest";
import { mkdtemp } from "node:fs/promises"; import { tmpdir } from "node:os"; import { join } from "node:path";
import { RulePolicyEngine } from "../../packages/policy/src/index.js"; import { LocalSandbox } from "../../packages/sandbox/src/index.js"; import { ToolRegistry, registerBuiltinTools } from "../../packages/tool-runtime/src/index.js"; import { WorkspaceBoundary } from "../../packages/workspace/src/index.js";
describe("shell classification", () => it("does not trust a caller to classify destructive commands as safe", async () => { const root = await mkdtemp(join(tmpdir(), "harness-")); const registry = new ToolRegistry(); registerBuiltinTools(registry); const context = { workspace: new WorkspaceBoundary(root), sandbox: new LocalSandbox(), policy: new RulePolicyEngine({ shell: "allow" }) }; await expect(registry.invoke("shell", { command: { argv: ["rm", "-rf", "x"] } }, context)).rejects.toThrow("Policy ask"); }));
