import { describe, expect, it } from "vitest";
import { RulePolicyEngine } from "../../packages/policy/src/index.js"; import { LocalSandbox } from "../../packages/sandbox/src/index.js"; import { ToolRegistry, registerBuiltinTools } from "../../packages/tool-runtime/src/index.js"; import { WorkspaceBoundary } from "../../packages/workspace/src/index.js";
describe("Git mutation policy", () => it("requires approval even in permissive Git mode", async () => { const registry = new ToolRegistry(); registerBuiltinTools(registry); const context = { workspace: new WorkspaceBoundary(process.cwd()), sandbox: new LocalSandbox(), policy: new RulePolicyEngine({ git: "allow" }) }; await expect(registry.invoke("git_commit", { message: "test", paths: ["README.md"] }, context)).rejects.toThrow("Policy ask"); await expect(registry.invoke("git_switch", { branch: "main" }, context)).rejects.toThrow("Policy ask"); await expect(registry.invoke("git_checkout", { target: "HEAD" }, context)).rejects.toThrow("Policy ask"); await expect(registry.invoke("git_restore", { paths: ["README.md"] }, context)).rejects.toThrow("Policy ask"); }));
